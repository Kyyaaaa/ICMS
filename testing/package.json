{
  "name": "icms-e2e-tests",
  "version": "1.0.0",
  "description": "E2E Automated Testing Suite for ICMS using Playwright",
  "private": true,
  "scripts": {
    "test": "playwright test",
    "test:ui": "playwright test --ui",
    "test:headed": "playwright test --headed",
    "test:guest": "playwright test guest/",
    "test:learner": "playwright test learner/",
    "codegen": "playwright codegen http://localhost:5173",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0",
    "@types/node": "^20.11.0"
  }
}
